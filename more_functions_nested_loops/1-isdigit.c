#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char c;

 int _isupper(int c)
{
	if (c >= '0' && c <= 'a')
		return (1);
	return (0);
}
